import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  input: {
    borderWidth: 1,
    borderRadius: 8,
    borderColor: '#DADADA',
    backgroundColor: '#F6F6F6',
    paddingLeft: 10,
  },
});
